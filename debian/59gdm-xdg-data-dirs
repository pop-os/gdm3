# If we are running GDM, update XDG data dirs

if [ "$GDMSESSION" ]; then
  if [ -z "$XDG_DATA_DIRS" ]; then
    XDG_DATA_DIRS=/usr/share/gdm
  else
    XDG_DATA_DIRS="$XDG_DATA_DIRS":/usr/share/gdm
  fi
  export XDG_DATA_DIRS
fi
